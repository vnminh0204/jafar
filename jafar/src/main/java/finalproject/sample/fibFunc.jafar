Program fib;

bool result;

func fib(int n): int {
    int res1;
    if (n <= 1): {
        res1 := 1;
    } else {
         res1 := fib(n-2) + fib(n-1);
    }
    return res1;
}

{
    int i;
    i := 2;
    result := true;
    while (i <= 7) :{
        result := result and (fib(i)== fib(i-1)+ fib(i-2));
        i := i+1;
    }
    print(result);
}
