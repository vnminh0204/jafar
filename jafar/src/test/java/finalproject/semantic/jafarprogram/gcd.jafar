Program gcd;

int x, y,ans;

{
    x:= 60;
    y:= 36;
    while x != y :
    {
        if x > y: {
            x := x-y;
        } else {
            y := y-x;
        }
    }
    //expected result will be 12
    ans := x;
    print(ans);
}
